cmake_minimum_required(VERSION 2.8)
project( ObjectTracker )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( ObjectTracker ObjectTracker.cpp )
target_link_libraries( ObjectTracker ${OpenCV_LIBS} )